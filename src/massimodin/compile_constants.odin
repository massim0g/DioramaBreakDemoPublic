package massimodin //@nested-tags:_main

//to adjust the CLI flags used when building, see src/build_config.json

ON_SWITCH :: #config(ON_SWITCH, false)
ON_PC :: !ON_SWITCH //hack, fix later
ON_WINDOWS :: !ON_SWITCH //hack, fix later